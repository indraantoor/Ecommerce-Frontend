import styled from 'styled-components';

export const NavContentFooterLayout = styled.div`
  display: flex;
  min-height: 100vh;
  flex-direction: column;
`;

export const MainContent = styled.main`
  flex-grow: 1;
  margin-top: 54px;
`;
