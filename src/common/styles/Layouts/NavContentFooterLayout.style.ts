import styled from 'styled-components';

export const NavContentFooterLayout = styled.main`
  display: flex;
  min-height: 100vh;
  flex-direction: column;
`;

export const MainContent = styled.div`
  flex-grow: 1;
  margin-top: 54px;
`;
